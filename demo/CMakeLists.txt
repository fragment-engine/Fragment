CMAKE_MINIMUM_REQUIRED(VERSION 3.3)

ADD_EXECUTABLE(FragmentDemo main.cpp)

TARGET_INCLUDE_DIRECTORIES(FragmentDemo PRIVATE ${Fragment_INCLUDE_DIR})

TARGET_LINK_LIBRARIES(FragmentDemo PUBLIC Fragment)